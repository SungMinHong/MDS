# overloading overriding

## 오버로딩(overloading)
- 오버로딩은 같은 클래스내에서 같은 이름의 메서드를 사용하는 것이다.
- 단, 매개변수 타입이 다르거나 매개변수 개수가 달라야 한다.
- 오버로딩은 대상이 되는 함수를 컴파일 시점에 지정하고

## 오버라이딩(overriding)
- 오버라이딩은 부모 class에서 정의한 메서드를 자식 class에서 변경하는 것이다.
- 단, 리턴 타입이 같아야 합니다.
- 오버라이딩은 런타임 시점에 정의한다.
